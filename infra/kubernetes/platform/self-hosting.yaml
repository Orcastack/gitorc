apiVersion: v1
kind: ConfigMap
metadata:
  name: gitorc-self-hosting
data:
  bootstrap-mode: sovereign-private-cloud
  upgrade-strategy: rolling
  self-monitoring: prometheus-grafana
  operator-panels: control-panel,repositories,clusters,pipelines,monitoring,identity,cloud-tools
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: gitorc-self-monitor
spec:
  schedule: "*/15 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          serviceAccountName: gitorc-runner
          containers:
            - name: self-monitor
              image: curlimages/curl:8.8.0
              command:
                - /bin/sh
                - -c
                - |
                  echo "Checking GITORC self-hosting endpoints" && \
                  echo "gateway=https://gitorc.internal.example/healthz" && \
                  echo "grafana=https://grafana.internal.example" && \
                  echo "prometheus=https://prometheus.internal.example"
